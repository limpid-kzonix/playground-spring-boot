package com.omniesoft.commerce.persistence.entity.enums;

/**
 * Used in persistence as ordinal enum type
 * Be careful : Never change the order of values
 */
public enum OAuthClient {
    FACEBOOK,
    GOOGLE
}
